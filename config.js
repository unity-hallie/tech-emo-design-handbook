// Single source of truth for handbook-wide values that may change once
// the project has a public home. Edit this file when a real URL exists.
//
// To use in a page:  <script src="./config.js"></script>  before the page's
// own scripts; values become available as window.HANDBOOK_CONFIG.

window.HANDBOOK_CONFIG = {
  // Where readers can find the handbook itself. Placeholder until a real
  // public URL exists. When the share-button copies a doll + instructions,
  // this URL is appended so the recipient knows where it came from.
  homeUrl: 'https://example.example/technical-emotion-design',

  // Short human-readable name for the handbook used in shared text.
  shortName: 'Technical Emotion Design',
};
